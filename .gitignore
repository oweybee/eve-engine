# dependencies
node_modules

# environment (contains Supabase service-role key, Betfair + Odds API keys — never commit)
.env

# macOS + runtime logs
.DS_Store
*.log

# X posting state (tracks which signal IDs have been tweeted)
.x_state.json

# env copies / shell accidents
.env.*
"cat .env"
_csv_cache.csv.gz

# Betfair VPS local secrets
scripts/betfair-vps/.env

# Training corpus cache (rebuild with ensemble/build_training_corpus.py)
_csv_cache_v3.csv.gz
__pycache__/
